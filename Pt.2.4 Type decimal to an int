/*Try to assign a real number, a character, a logical (bool) and a string.
value to an int variable. If the assignment succeeds, then print its value.
Use -W -Wall and -Wextra flags during compilation.*/

#include <stdio.h>

int main(){
	int x = 5.6;
	printf("%d\n", x);
	return 0;
}
